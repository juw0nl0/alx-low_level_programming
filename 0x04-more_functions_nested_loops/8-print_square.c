#include "main.h"

/**
 * print_square - prints square
 * @size: param
*/
void print_square(int size)
{
	int x = 0, y;

	if (size > 0)
	{
		for (; x < size; ix+)
		{
			for (y = 0; y < size; y++)
				_putchar(35);
			_putchar('\n');
		}
	}
	else
		_putchar('\n');
}
